<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="wrap_content"
    android:layout_height="190dp"
    android:background="@drawable/hcmut"
    android:orientation="vertical"
    android:theme="@style/ThemeOverlay.AppCompat.Dark">

    <de.hdodenhof.circleimageview.CircleImageView xmlns:app="http://schemas.android.com/apk/res-auto"
        android:id="@+id/profile_image"
        android:layout_width="70dp"
        android:layout_height="70dp"
        android:layout_above="@id/txtName"
        android:layout_alignParentStart="true"
        android:layout_alignParentLeft="true"
        android:layout_centerVertical="true"
        android:layout_marginStart="24dp"
        android:layout_marginLeft="24dp"
        android:src="@drawable/profile" />

    <TextView
        android:id="@+id/txtName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/txtAccountBalance"
        android:layout_alignStart="@+id/profile_image"
        android:layout_alignLeft="@+id/profile_image"
        android:gravity="left"
        android:paddingBottom="4dp"
        android:text="Name"
        android:textColor="#FFF"
        android:textSize="14sp"
        android:textStyle="bold"
        android:background="#001CD4"/>

    <TextView
        android:id="@+id/txtAccountBalance"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/txtEmail"
        android:layout_alignStart="@+id/txtName"
        android:layout_alignLeft="@+id/txtName"
        android:layout_marginBottom="8dp"
        android:gravity="left"
        android:text="Balance"
        android:textColor="#fff"
        android:textSize="14sp"
        android:background="#001CD4"/>

    <TextView
        android:id="@+id/txtEmail"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignStart="@+id/txtAccountBalance"
        android:layout_alignLeft="@+id/txtAccountBalance"
        android:layout_alignParentBottom="true"
        android:layout_marginBottom="8dp"
        android:gravity="left"
        android:text="Email"
        android:textColor="#fff"
        android:textSize="14sp"
        android:background="#001CD4"/>
</RelativeLayout>